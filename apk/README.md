This folder is no longer used for committing APKs.

APKs are built and distributed via GitHub Actions artifacts (and optionally GitHub Releases).

See:
- `.github/workflows/android-debug-apk.yml`
- Repo root `README.md` section "Getting the APK (CI artifacts)"
